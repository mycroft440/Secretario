<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      OEM/system-only distribution. Declaring CALL_AUDIO_INTERCEPTION does not
      grant it to a normal APK; the platform/OEM must grant the SystemApi
      permission according to its signing/privapp policy. ProtectedPermissions
      is suppressed only on this OEM manifest because requesting it is the
      explicit purpose of this non-public flavor.
    -->
    <uses-permission
        android:name="android.permission.CALL_AUDIO_INTERCEPTION"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- The privileged PSTN build is intentionally restricted to telephony devices. -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <application>
        <activity
            android:name=".telecom.PrivilegedDialerActivity"
            android:exported="true"
            android:label="CallGuard AI — Telefone">
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="tel" />
            </intent-filter>
        </activity>

        <service
            android:name=".telecom.PrivilegedInCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true"
            android:foregroundServiceType="microphone">
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_CAR_MODE_UI"
                android:value="false" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>
    </application>
</manifest>
